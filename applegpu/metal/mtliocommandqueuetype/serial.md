# MTLIOCommandQueueType.serial

*Case · iOS 16.0, iPadOS 16.0, Mac Catalyst 16.0, macOS 13.0, tvOS 16.0, visionOS 1.0*

<https://developer.apple.com/documentation/metal/mtliocommandqueuetype/serial>

Sets a new input/output command queue’s type to a queue that runs commands serially.

## Declaration

```swift
case serial
```

## See also

### I/O command queue types
- [MTLIOCommandQueueType.concurrent](https://developer.apple.com/documentation/metal/mtliocommandqueuetype/concurrent) — Sets a new input/output command queue’s type to a queue that runs commands concurrently.
